import React from "react";
import { Button } from "@material-ui/core";
const Buttons = ({ title, onCLick, disabled, onhover }) => {
  return (
    <div>
      <Button variant=""></Button>
    </div>
  );
};

export default Buttons;
